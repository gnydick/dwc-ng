{
  "name": "@dwc-ng/mock-duet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Mock RRF server speaking the rr_ dialect, with scripted scenarios (CLAUDE.md Task #3).",
  "scripts": {
    "start": "node --title=mock-duet src/cli.ts",
    "dev": "node --title=mock-duet --watch src/cli.ts",
    "ctl": "node src/mockctl.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "~6.0.2"
  }
}
