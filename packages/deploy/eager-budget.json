{
  "maxEagerBytes": 483328,
  "note": "472 KiB. SECOND raise on 2026-08-22/23, for the decay viewer: measured branch 474,033 B (baseline at that point 460,084 B, +13,949 B, JS ~11 KB and CSS ~2.3 KB; uPlot was already eager via TemperatureChart so none of it is the chart library). NOTE the previous note's '9,895 B headroom' was already false when written - main had grown to 460,084 B, leaving 716 B. Do not trust a stale headroom figure; measure. New headroom 9,295 B, and work items E2 (canvas heatmap), F (residual chart, custom editor) and G (verify bars, apply diff) all still have to fit in it. This is the point at which lazy-loading the Shaping screen stops being optional. RAW bytes, because DSF's Kestrel serves uncompressed. Raising this is a deliberate commit, not a fix."
}
