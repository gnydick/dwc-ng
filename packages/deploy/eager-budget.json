{
  "maxEagerBytes": 484352,
  "note": "473 KiB. THIRD raise, 2026-08-24, for #68's interpretation layer (evidence machine, findings, walk, apply). Measured branch 483,506 B against the previous ceiling of 483,328 B — over by 178 B, i.e. this raise buys 846 B of headroom and nothing more. RAW bytes; DSF's Kestrel serves uncompressed. The growth is NOT the Shaping cards, which are already a lazy chunk (ShapingCards ~78 KB): it is compose/services.ts, which is eager and now imports shaping/evidence/{caveat,evidence,findings} plus shaping/copy.ts, because the `products` memo lives on the service. The previous note said 'this is the point at which lazy-loading the Shaping screen stops being optional' and that remains true and is now overdue — the eager cost is the SERVICE's dependency on the findings layer, so the fix is to move the shaping service (or its products memo) behind the same dynamic import its cards already sit behind, not to trim bytes. Filed as its own ticket rather than done here, because it is a compose-layer refactor and this raise was blocking a deploy. Do not raise again without doing it: 846 B is one caveat arm. Do not trust this headroom figure later - measure."
}
