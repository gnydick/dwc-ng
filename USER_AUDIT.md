| Badge              | Meaning                                                                          |
|:-------------------|:---------------------------------------------------------------------------------|
| ✅ **FIXED**       | Fixed in dwc-ng (file / commit cited)                                            |
| 🟡 **PARTIAL**     | Exists but approximated / incomplete / missing sub-cases                         |
| ❌ **TODO**        | Not present — net-new work                                                       |
| 🟦 **DEFERRED**    | Implemented on a branch but needs architecture reconciliation before re-applying |
| 🔬 **CORNER CASE** | A specific edge case that is easy to get wrong; verify with a fixture            |

| Status | Bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
|--------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ✅     | re-probe for bed must subtract the probe's **Z** trigger height (G31 Z) to get the spot's map value — the probe doesn't sit at Z=0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `heightmapValue = stopHeight − triggerHeight`, live `sensors.probes[0].triggerHeight`; `heightmap/probeReply.ts` + `cards/BedCards.tsx` (da16b87)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ✅     | file-list scroll position not restored on return / reload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | gate the scroll-restore on `!browser.loading()` so the one-shot offset isn't consumed onto the empty loading list (which clamps it to 0); `files/FileBrowserView.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 🔬     | **M671 V position — tested live 2026-07-23, NOT persisted.** Sent `M671 X-3.4:-3.4:350.9 Y19.3:280.7:150 S7.5` (RAM only; `config-user.g` untouched, reverts on reboot). Five G32 runs after the change settled monotonically — deviation 0.006 → 0.003 → 0.002 → **0.001 → 0.001**, adjustment spread 0.013 → 0.006 → 0.005 → **0.002 → 0.002** — i.e. ~1 µm held across consecutive runs, 1/10 of a full motor step and 1/10 of what `M558 S0.01` demands. **Cause not isolated**: the pre-change pair ran 4 min after the bed was heated (mid-transient) while these ran ~2 h later thermally settled, so stable temperature explains the same improvement. Simulation says the 5.4 mm pivot error contributes ~0.1 µm here, i.e. nothing. The clean A/B — revert V to 275.3 in the *current* thermal state and re-run — was offered and not yet run. To persist the fix: edit `config-user.g:2`. |
| 🔬     | **suspected M671 error** (analysis): V's Y is probably wrong. `config-user.g` has `M671 X-3.4:-3.4:350.9 Y19.3:275.3:150`, but `bed.g`'s probe points (5,5) and (5,295) are exactly symmetric about Y=150, so a symmetric V would be **Y280.7**, not 275.3 — off by 5.4 mm. That value makes the probe→screw distances identical (16.58 mm both, vs 21.42 today). Both configured values ending `.3` suggests one was entered by pattern rather than measured. **Not yet acted on**: simulation shows the induced error is ~2% of *tilt* and exactly zero for a uniform offset, so it explains none of the observed tramming (which is dominated by the 10 µm full-step floor). Latent — a 1 mm tilt would leave ~19 µm. Confirming it by response needs ≥0.5 mm of deliberate single-screw tilt, which is a probe-safety decision.                                                                  |
| ✅     | bed maintenance screen: height map on it, tram controls, mesh controls like loading, unloading csvs, being able to choose which csv you load as a heightmap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Screen renamed **Bed maintenance** (id stays `bed` so saved layouts survive). **Mesh** card: CSV picker over `0:/sys/*.csv`, Probe (`G29`) / Use this map (`G29 S1 P`) / Save as (`G29 S3 P`) / Clear (`G29 S2`), plus live `move.compensation` status. **Bed tram** card: `G32` + `Home Z`, showing the last tram parsed from the reply log. **Height map**: 2D picker ↔ rotatable 3D surface (bicubic, blue→cyan→green→yellow→red, scale legend, clickable probe spheres, absolute vertical scale). **Probe point**: reading taken from `sensors.probes[].lastStopHeight` so a macro outliving the request timeout is not lost, trigger-height subtraction shown as working, and unsaved edits now warn before a reload discards them. `heightmap/store.ts` carries the loaded path so Save writes back to — and reloads — the file being edited. Leadscrew-position solver dropped by request (2026-07-23). |
| ✅     | card-head copy tips silently dead on every non-localhost origin — and would be permanently dead once served from the SD card over plain HTTP (`navigator.clipboard` is SecureContext-only; the old bare `catch {}` hid the failure)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | shared `shell/copyText.ts` (modern API → offscreen-textarea `execCommand` fallback), and `CardTip` now shows **"Can't copy"** on failure instead of swallowing it; 6 tests against synthetic globals since no browser reproduces an insecure origin (5dc83dc)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ✅     | imported / deleted / edited screens & cards vanished on reload; same-name screen import stacked duplicates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `apply()` — the one mutation path — now caches on every edit with a `dirty` flag, and `loadFromMachine` refuses to clobber unsaved local work (a clean session still adopts SD); same-name screen import **replaces** rather than duplicating; deleting a screen navigates away instead of stranding on the dead hash; `config/store.ts` + `compose/ComposedScreen.tsx` (a0a1037)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ✅     | the camera stream never stops even after the camera is disabled at the page level. big bandwidth waste                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `<img>` unmount does NOT close a `multipart/x-mixed-replace` connection — Solid removed the element correctly and the stream kept running. Measured live 2026-07-24: pin→unpin→pin toggled the element in and out of the DOM while resource timing showed exactly ONE entry with `responseEnd === 0`; re-showing never opened a new request because the old one had never ended, so a hidden camera streamed for as long as the tab was open. Fix: `StreamImage` in `shell/CameraPanel.tsx` owns the element AND its teardown, pointing `src` at a 1×1 data URI on cleanup (not `src=""`, which some browsers resolve against the document URL and re-fetch the page). Tied to the element's lifetime, so it also stops when the URL is cleared in Settings while the panel stays mounted. Verified live: re-pin now needs a NEW request each time, and the retained element's src reads the data URI after unpin. |
| ✅     | change behavior of card overlap: if cards are redesigned and dimensions change, the next time the page loads, push the cards right or down, order of cards from left to right and down. choose right or down based on whichever direction has the least distance to cover                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `mergeCanvas` used to let localStorage win outright, so a card redesigned taller kept its old height on every browser that had ever laid the screen out and its new content rendered below the fold — the 2026-07-24 case, where `position` 95→103 and `active-job` 40→46 changed nothing. Now `growToDefaults` takes the larger span per axis without moving anything, then `reflow` places cards in reading order and pushes each collider right or down by whichever penetration is fewer **grid cells** (ties go right, the bounded axis). The topmost-leftmost card never moves, so a grown card keeps its spot and its neighbours yield — that falls out of the ordering, not a special case. Gated on an actual growth, which is correctness not speed: a hidden card releases its cells so a visible one can be resized into them, storing a *legal* overlap that must not be "fixed". Terminates and is idempotent by construction. `shell/panelCanvas.ts`; 15 tests incl. a red check; spec `docs/superpowers/specs/2026-07-25-card-overlap-reflow-design.md` (347f615) |
| ✅     | prompt for save name when save to machine is clicked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `saveToMachine(connector, label?)` forwards the name to the snapshot the save already took, so **Saved versions** says what a revert would restore instead of ten rows of `saved`. Clicking **Save to machine** now arms an inline name field rather than saving outright — the same two-step shape as the file browser's rename and armed delete, this app's convention for a single text field (there is no modal for one input anywhere in the UI). Enter saves, Escape cancels, blank falls back to `saved` so the prompt can never block a save; geometry is captured at save time, not arm time, so edits made while the field is open are included. `snapshot()` is the sole place a snapshot is created, so the label rule lives there — trim, default when blank, cap at `MAX_LABEL_LEN` (60) — and a caller reaching the store directly is covered without knowing the rule exists. The label is **local-only** and never enters the payload: a named save and an unnamed one upload identical bytes, pinned by a test, so a name cannot become config that travels between machines. Measured live: card height 96px armed and unarmed, *Reset everything* does not move, and the field opens where the Save button was so the pointer lands in the input. `config/store.ts` + `cards/SettingsCards.tsx`; 3 tests; spec `docs/superpowers/specs/2026-07-25-named-config-backups-design.md` (be5d38e, 1167cda) |
| ✅     | lost the strict scroll hover area for large scroll hit targets like the console and text editors. almost the entire card causes the text area to scroll instead of the page. which makes navigation difficult. there should be an invisible explicit scroll area on the left and right sides of the text areas that will scroll the text area, otherise if the mouse is over the 70% center of the text area, it will scroll the page                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A wheel over the middle of a big scroller now moves the PAGE; only invisible strips at its left and right edges move the box. Applies to the two big TEXT surfaces only — the console and the CodeMirror editor — which opt in with `data-edge-scroll-host`. A first pass inferred it from size (any scroller over 180x100) and that was wrong: it captured ordinary cards` `.panel-body` and their file lists, so a rule meant for two surfaces changed the feel of the whole canvas. Size cannot tell a console from a card that merely has a lot in it, so the component says which it is; a test counts the opt-ins so a third is a deliberate act. Strip width is `clamp(24px, 15%, 30%)`: the 70% centre asked for, with a floor so a narrow card still has something hittable and a ceiling so the centre can never vanish. Hovering a governed box ghosts the strips in with a copper gradient running from the card edge inward — sized from the SAME `--edge-w` the handler uses, so the lit area is exactly the area that responds. Measured in Chrome on `.console-history` (1207x186, 181px strips): centre wheel scrolled the page 50→170 with the box untouched, both edges left `defaultPrevented` false. `shell/edgeScroll.ts` + `shell/Shell.tsx`; 8 tests | 
