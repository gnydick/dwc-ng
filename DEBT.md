# Debt

> **Generated by `packages/invariants` — DO NOT EDIT.**
> Declare a defect beside the broken code with an `@broken` block, or — when
> it has no code home — in `packages/invariants/debt.json`. Regenerate with
> `pnpm --filter @dwc-ng/invariants generate`.

Everything found broken and not fixed on the spot. Same badges and columns as
`USER_AUDIT.md`, which catalogues what the *operator* reported; this one
catalogues what the *code* turned out to be doing.

**10 catalogued · 5 still open.**

| Badge | Meaning |
|:------|:--------|
| ✅ **FIXED** | Fixed (file / commit cited) |
| 🟡 **PARTIAL** | Mitigated but incomplete — sub-cases remain |
| ❌ **TODO** | Known broken, not yet addressed |
| 🟦 **DEFERRED** | Deliberately postponed; the reason is the Fix column |
| 🔬 **CORNER CASE** | Narrow edge case that is easy to get wrong; verify with a fixture |

| Status | Bug | Fix |
|--------|-----|-----|
| ❌ | **`card-lab-state-pills-inert`** — Card Lab's state pills do not change the bench, so the layout audit measures ten cards EMPTY. Their real content — the case where a floor is most likely to be wrong — is never audited, while the sweep reports on all 43 as though coverage were uniform. Measured 2026-07-31 across the four-pitch sweep. This is what keeps `dev/card-floor-independent-of-size` at rung 4 rather than merely the sweep being manual. <br>`packages/ui/src/dev/LayoutAuditPanel.tsx` | — |
| ❌ | **`declared-vs-measured-oracle-missing`** — There is no oracle comparing a card's DECLARED natural size against its MEASURED floor. Invariants A and B both ask whether geometry is stable, and neither asks whether it is correct: a card whose declared size cannot contain its own content passes both while rendering clipped. <br>`packages/ui/src/dev/layoutAudit.ts` | — |
| ✅ | **`firmware-arm-bypasses-escape`** — Five two-step controls armed with a raw createSignal instead of createArmed, so Escape reached none of them — firmware flash (M997), heater-fault reset, per-object cancel, filament delete, and save-to-machine. control/armed.ts meanwhile stated that Escape disarms EVERY armed control "including ones written later by someone who never read this file". Reading the code during the 2026-07-31 sweep found ONE of the five; the walking test found all five. <br>`packages/ui/src/cards/FirmwareUpdateCard.tsx and four others` | All five converted to createArmed. test/armed.test.ts now walks src and rejects any [armed, …] signal not produced by createArmed, naming file and line — red-checked, it failed on all five before the fix. Promotes control/escape-disarms from rung 5 to 6. |
| ✅ | **`gcode-quote-bypassed-in-three-builders`** — Three command builders hand-wrote their own quotes instead of calling gcodeQuote, so an embedded quote was NOT doubled and closed the parameter early — the exact injection control/gcode-quoting exists to prevent, and which its own text described as already fixed. Measured before the change: print('0:/sys/a"b.g') emitted M32 "0:/sys/a"b.g" while runMacro on the same input correctly emitted M98 P"0:/sys/a""b.g". Filenames reach these from the SD listing and from operator uploads. Found 2026-08-01 by the COMPILER while promoting the invariant from rung 5 to 7 — not by review, and not by any test, in the months it was there. <br>`packages/ui/src/control/commands.ts (print, simulate, loadFilament)` | Command assembly moved behind a `gc` tagged template whose interpolations must be a branded `Param`, produced only by gcodeQuote (strings), n()/fixed() (numbers) or axisLetter (bare tokens). A raw string in a command literal is now a compile error, which is what surfaced all three. A/B over all 45 builders: 39 byte-identical, 6 changed and every change a fix. 5 regression tests. |
| 🟡 | **`invariant-b-names-positions-not-culprits`** — Invariant B reports drift by POSITION, so the audit panel gives a count of drifting descendants rather than naming which element moved. A reader has to hunt for the culprit, which in practice means the finding is noted and not acted on. <br>`packages/ui/src/dev/layoutAudit.ts:152` | Mitigated only in that the count is at least correct and ranks violations first. Real fix: report a stable path to the drifting node. |
| ✅ | **`job-buttons-swallow-failures`** — Pause, Resume and Cancel were raw <button>s calling void sendCode(...) with no catch and no acknowledgement, so a rejected code or a blocking write guard left the operator believing a running print was paused or cancelled when nothing was sent. The last three buttons the 2026-07-22 audit left on raw elements, still unconverted 136 commits later. <br>`packages/ui/src/cards/ActiveJobCard.tsx` | Converted to GcodeButton, whose acknowledgement follows the connector PROMISE rather than the click. The job-toggle width reservation is kept, so Cancel still cannot slide under the pointer on a state change. |
| ❌ | **`layout-audit-single-viewport`** — The layout sweep runs at one viewport width. Media queries add no specificity and every desktop-width measurement sits above the breakpoints, so the entire class of narrow-viewport bugs — the one that produced 'fine on the dev server, broken on the printer' and cost four wrong diagnoses — is invisible to it by construction. <br>`packages/ui/src/dev/LayoutAuditPanel.tsx` | — |
| ❌ | **`mock-duet-seqs-optional`** — `pnpm typecheck` fails at the workspace level: four TS18048 errors, `'machine.seqs.move' is possibly 'undefined'` and the same for the `before`/`afterLoad` locals, under `noUncheckedIndexedAccess`. Pre-existing — confirmed by checking out `main` and running it there, where it fails identically. Found 2026-07-31 while verifying the invariants package, which typechecks clean itself. <br>`packages/mock-duet/test/simulation.test.ts:141,145` | — |
| ✅ | **`qoi-header-unbounded-allocation`** — A corrupt QOI thumbnail header could crash the tab. The 32-bit width/height were assembled with `<< 24`, which is SIGNED in JS, so a width of 0xFF______ read negative and `new Uint8ClampedArray(negative)` threw RangeError; and a width of 0x7Fffffff produced a **pixelCount of 2.1 billion and an 8.6 GB allocation** — measured 2026-08-01, it did not throw, it just took the memory. The bytes come off the SD card via rr_thumbnail, so a damaged or truncated preview reached this directly, and `Thumbnail.tsx` had no catch: the throw escaped a Solid effect and the job listing went down with the decoration. The header's own claim of 'malformed input can only yield wrong pixels, never NaN or a crash' was false in both directions. <br>`packages/ui/src/thumbnails/qoi.ts:66` | Dimensions read UNSIGNED (`>>> 0`), and pixelCount is bounded by what the remaining bytes could physically encode — QOI_OP_RUN carries at most 62 pixels in one byte, so `(bytes.length - 14) * 62` is the true ceiling rather than an arbitrary cap, and every valid image passes it. `Thumbnail.tsx` catches and leaves the canvas blank. Declared as `thumbnails/decode-allocates-only-what-the-stream-could-hold` (rung 6); 3 tests, each red-checked against the pre-fix decoder. |
| ✅ | **`sanitize-canvas-accepted-a-prototype`** — sanitizeCanvas — the function whose whole job is making stored layout JSON safe — walked it with raw Object.entries and assigned out[id]. JSON.parse creates "__proto__" as an OWN property, so that key ran the prototype SETTER rather than adding an entry. Measured 2026-08-01: a stored {"__proto__":{col,row,colSpan,rowSpan}, ...} came back with Object.getPrototypeOf(out) replaced by the attacker's object, while Object.keys(out) showed only the legitimate card and nothing looked wrong. Global Object.prototype was NOT polluted, so the blast radius is the returned CanvasState, and the source is this browser's own localStorage — but it is the exact class the util/safeObject kernel exists to delete, in the one function named for preventing it. <br>`packages/ui/src/shell/panelCanvas.ts:813` | safeEntries at that walk, plus the rule made self-maintaining: test/safe-walks.test.ts fails any file that imports safeEntries and also uses raw Object.entries, so the import that makes a boundary safe is the one that arms the check. safeEntries is now generic, so converting a trusted walk costs nothing and the ban can be total. Audit of all 18 raw walks in src: 17 safe (locally-built records, or keys gated by parseCardId/isSafeKey, or already filtered upstream), this one not. Declared at rung 4, up from 3; 2 tests, both red-checked. |
