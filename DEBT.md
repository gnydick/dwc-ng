# Debt

> **Generated by `packages/invariants` — DO NOT EDIT.**
> Declare a defect beside the broken code with an `@broken` block, or — when
> it has no code home — in `packages/invariants/debt.json`. Regenerate with
> `pnpm --filter @dwc-ng/invariants generate`.

Everything found broken and not fixed on the spot. Same badges and columns as
`USER_AUDIT.md`, which catalogues what the *operator* reported; this one
catalogues what the *code* turned out to be doing.

**7 catalogued · 7 still open.**

| Badge | Meaning |
|:------|:--------|
| ✅ **FIXED** | Fixed (file / commit cited) |
| 🟡 **PARTIAL** | Mitigated but incomplete — sub-cases remain |
| ❌ **TODO** | Known broken, not yet addressed |
| 🟦 **DEFERRED** | Deliberately postponed; the reason is the Fix column |
| 🔬 **CORNER CASE** | Narrow edge case that is easy to get wrong; verify with a fixture |

| Status | Bug | Fix |
|--------|-----|-----|
| ❌ | **`card-lab-state-pills-inert`** — Card Lab's state pills do not change the bench, so the layout audit measures ten cards EMPTY. Their real content — the case where a floor is most likely to be wrong — is never audited, while the sweep reports on all 43 as though coverage were uniform. Measured 2026-07-31 across the four-pitch sweep. This is what keeps `dev/card-floor-independent-of-size` at rung 4 rather than merely the sweep being manual. <br>`packages/ui/src/dev/LayoutAuditPanel.tsx` | — |
| ❌ | **`declared-vs-measured-oracle-missing`** — There is no oracle comparing a card's DECLARED natural size against its MEASURED floor. Invariants A and B both ask whether geometry is stable, and neither asks whether it is correct: a card whose declared size cannot contain its own content passes both while rendering clipped. <br>`packages/ui/src/dev/layoutAudit.ts` | — |
| ❌ | **`firmware-arm-bypasses-escape`** — This card arms with a raw createSignal instead of control/armed.ts's createArmed, so Escape does not disarm it — while armed.ts states that Escape disarms "EVERY armed control on the page, including ones written later by someone who never read this file". This card is that someone, and its next click sends M997: a firmware flash, the most destructive action in the app. A Cancel button exists, so there IS a way out, but not the universal one the module promises. <br>`packages/ui/src/cards/FirmwareUpdateCard.tsx:25` | replace with `const [armed, setArmed] = createArmed<true>()`, which registers with the single capture listener. That also promotes control/escape-disarms from rung 5 to 6, since createArmed becomes the only route to an armed control. |
| 🟡 | **`invariant-b-names-positions-not-culprits`** — Invariant B reports drift by POSITION, so the audit panel gives a count of drifting descendants rather than naming which element moved. A reader has to hunt for the culprit, which in practice means the finding is noted and not acted on. <br>`packages/ui/src/dev/layoutAudit.ts:152` | Mitigated only in that the count is at least correct and ranks violations first. Real fix: report a stable path to the drifting node. |
| ❌ | **`job-buttons-swallow-failures`** — Pause, Resume and Cancel are raw <button>s calling `void app.connector.sendCode(...)` with no catch and no acknowledgement, so a rejected code or a blocking write guard leaves the operator believing a running print was paused or cancelled when nothing was sent. GcodeButton exists for exactly this and says so in its own header: "a click that reports success while the command never left would be worse than no feedback at all". These are the last three buttons the 2026-07-22 audit left on raw elements, still unconverted 136 commits later. <br>`packages/ui/src/cards/ActiveJobCard.tsx:95` | convert to GcodeButton, whose acknowledgement follows the connector's promise rather than the click. Keep the job-toggle width reservation — it is what stops Cancel sliding under the pointer on a state change. |
| ❌ | **`layout-audit-single-viewport`** — The layout sweep runs at one viewport width. Media queries add no specificity and every desktop-width measurement sits above the breakpoints, so the entire class of narrow-viewport bugs — the one that produced 'fine on the dev server, broken on the printer' and cost four wrong diagnoses — is invisible to it by construction. <br>`packages/ui/src/dev/LayoutAuditPanel.tsx` | — |
| ❌ | **`mock-duet-seqs-optional`** — `pnpm typecheck` fails at the workspace level: four TS18048 errors, `'machine.seqs.move' is possibly 'undefined'` and the same for the `before`/`afterLoad` locals, under `noUncheckedIndexedAccess`. Pre-existing — confirmed by checking out `main` and running it there, where it fails identically. Found 2026-07-31 while verifying the invariants package, which typechecks clean itself. <br>`packages/mock-duet/test/simulation.test.ts:141,145` | — |
