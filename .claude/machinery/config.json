{
  "worktree": "always",
  "components": {
    "connector": "packages/connector",
    "deploy": "packages/deploy",
    "invariants": "packages/invariants",
    "mock-duet": "packages/mock-duet",
    "ui": "packages/ui"
  },
  "tiers": {
    "fast": "node tools/tier-fast.mjs <components>",
    "merge": "node tools/tier-merge.mjs",
    "heavy": "pnpm --filter @dwc-ng/mock-duet test:slow",
    "declaration": "directory: <component>/test/*.test.ts is fast; <component>/test/slow/*.test.ts runs at push to main only",
    "assignment": "claude-decides"
  },
  "comparisonAgent": "we have no comparison for this project",
  "reviewBeforeMain": "person-and-agent"
}
